package com.oums.util;

import org.apache.log4j.LogManager;
import org.apache.log4j.Logger;

public class BaseConverter<V, P> {

	private static Logger logger = LogManager.getLogger(BaseConverter.class.getName()); 
	
	
}
